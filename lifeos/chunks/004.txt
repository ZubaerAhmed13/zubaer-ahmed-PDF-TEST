:'Moderate',class:'status-warn',score}:{label:'Low Risk',class:'status-ok',score}}
 taskScore(t,ctx={}){if(['Completed','Cancelled','Someday','Waiting'].includes(t.status))return-999;if((t.blockedBy||[]).length)return-500;let s=PRIORITY_WEIGHT[t.priority]||20;s+=({High:18,Medium:10,Low:4}[t.importance]||10);const risk=this.taskRisk(t);s+=risk.score*.55;if(t.deadline){const d=daysBetween(localDate(),t.deadline);s+=d<0?40:d<=1?32:d<=3?22:d<=7?12:0}if(ctx.energy){const map={High:9,Medium:6,Low:3};const target=map[t.energyRequired||'Medium'],cur=ctx.energy;s+=Math.max(0,10-Math.abs(target-cur)*2)}if(ctx.minutes&&Number(t.estimatedDuration||45)<=ctx.minutes)s+=8;if(t.postponeCount)s+=Math.min(10,t.postponeCount*2);return s}
 async capacity(date=localDate()){const s=await this.r.settings(),d=await this.data();const dayStart=minutes(s.dayStart),dayEnd=minutes(s.dayEnd),awake=Math.max(0,dayEnd-dayStart);const ev=d.events.filter(e=>e.startDate===date);let fixed=0;for(const e of ev)fixed+=Math.max(0,minutes(e.endTime)-minutes(e.startTime))+Number(e.travelBefore||0)+Number(e.travelAfter||0)+Number(e.preparationTime||0);const blocks=d.blocks.filter(b=>b.date===date);const scheduled=blocks.reduce((a,b)=>a+Number(b.duration||0),0);const buffer=Math.max(Number(s.minBufferMinutes||45),Math.round(awake*Number(s.bufferPercent||18)/100));const flex=Math.max(0,awake-fixed-buffer);const used=scheduled;const pct=flex?used/flex*100:used?200:0;const th=s.capacityThresholds||defaultSettings.capacityThresholds;const label=pct<=th.flexible?'Flexible':pct<=th.balanced?'Balanced':pct<=th.busy?'Busy':pct<=th.full?'Full':'Overloaded';return{awake,fixed,buffer,flex,scheduled,remaining:flex-used,pct,label}}
 async renderDashboard(){const d=await this.data(),s=await this.r.settings(),today=localDate(),am=this.areaMap(d.areas);const cap=await this.capacity(today);const todaysBlocks=d.blocks.filter(b=>b.date===today);const todaysTasks=d.tasks.filter(t=>t.preferredDate===today||todaysBlocks.some(b=>b.taskId===t.id));const done=d.tasks.filter(t=>t.status==='Completed'&&t.completedAt?.startsWith(today));const upcoming=[...d.events.filter(e=>e.startDate>=today).map(x=>({type:'Event',date:x.startDate,title:x.title})),...d.tasks.filter(t=>t.deadline&&t.deadline>=today&&t.status!=='Completed').map(x=>({type:'Deadline',date:x.deadline,title:x.title}))].sort((a,b)=>a.date.localeCompare(b.date)).slice(0,6);const rec=[...d.tasks].filter(t=>!['Completed','Cancelled','Someday','Waiting'].includes(t.status)).sort((a,b)=>this.taskScore(b)-this.taskScore(a)).slice(0,5);const weekly=await this.balanceStats(d);const pressure=this.pressure(d,7);const sessions=d.sessions.filter(x=>x.date===today);const actual=sessions.reduce((a,x)=>a+Number(x.actualMinutes||0),0);const planQ=this.planQuality(cap,d,today);return `<div class="grid g4"><div class="card"><div class="muted">Today capacity</div><div class="metric">${Math.round(cap.pct)}%</div><div class="${cap.pct>105?'status-bad':cap.pct>80?'status-warn':'status-ok'}">${cap.label}</div></div><div class="card"><div class="muted">Scheduled</div><div class="metric">${hm(cap.scheduled)}</div><div class="muted">${hm(Math.max(0,cap.remaining))} flexible left</div></div><div class="card"><div class="muted">Completed today</div><div class="metric">${done.length}</div><div class="muted">${d.tasks.length} total tasks</div></div><div class="card"><div class="muted">Plan quality</div><div class="metric">${planQ}%</div><div class="muted">schedule realism</div></div></div>
<div class="grid g2" style="margin-top:16px"><div class="card"><div class="section-head"><h2>Top priorities</h2><div class="actions"><button class="btn small primary" data-action="what-now">What should I do now?</button><button class="btn small" data-action="generate-day">Generate my day</button></div></div>${this.taskList(rec,am,{compact:true})}</div><div class="card"><div class="section-head"><h2>Upcoming pressure</h2></div><div class="kpi-row"><div class="kpi"><b>${pressure.deadlines}</b><div class="muted">deadlines / 7d</div></div><div class="kpi"><b>${pressure.events}</b><div class="muted">events / 7d</div></div><div class="kpi"><b>${hm(pressure.work)}</b><div class="muted">required task time</div></div></div><div class="note" style="margin-top:12px">Risk: <b class="${pressure.risk==='High'?'status-bad':pressure.risk==='Moderate'?'status-warn':'status-ok'}">${pressure.risk}</b></div></div></div>
<div class="grid g2" style="margin-top:16px"><div class="card"><div class="section-head"><h2>Upcoming</h2></div>${upcoming.length?`<div class="list">${upcoming.map(x=>`<div class="item"><div class="grow"><div class="item-title">${escapeHTML(x.title)}</div><div class="meta"><span>${x.type}</span><span>${fmtDate(x.date)}</span></div></div></div>`).join('')}</div>`:'<div class="empty">Nothing urgent is coming up.</div>'}</div><div class="card"><div class="section-head"><h2>Life balance — this week</h2></div>${weekly.slice(0,6).map(x=>`<div style="margin:10px 0"><div class="split"><span>${escapeHTML(x.icon)} ${escapeHTML(x.name)}</span><span class="muted">${hm(x.actual)} / ${x.target}h</span></div><div class="progress"><span style="width:${clamp(x.actual/(x.target*60||1)*100,0,100)}%"></span></div></div>`).join('')}</div></div>
<div class="grid g3" style="margin-top:16px"><div class="card"><div class="muted">Focus today</div><div class="metric">${hm(actual)}</div><div class="muted">${sessions.length} sessions</div></div><div class="card"><div class="muted">Protected buffer</div><div class="metric">${hm(cap.buffer)}</div><div class="muted">not treated as work time</div></div><div class="card"><div class="muted">Current mode</div><div class="metric" style="font-size:20px">Local-first</div><div class="muted">All data stays in this browser</div></div></div>`}
 planQuality(cap,d,date){let score=100;if(cap.pct>105)score-=30;else if(cap.pct>95)score-=15;if(cap.buffer<45)score-=10;const ev=d.events.filter(x=>x.startDate===date).sort((a,b)=>a.startTime.localeCompare(b.startTime));for(let i=1;i<ev.length;i++)if(minutes(ev[i].startTime)<minutes(ev[i-1].endTime))score-=15;const critical=d.tasks.filter(t=>t.deadline===date&&t.status!=='Completed').length;score-=Math.min(20,critical*5);return clamp(Math.round(score),0,100)}
 pressure(d,days){const today=localDate(),end=addDays(today,days);const deadlines=d.tasks.filter(t=>t.deadline&&t.deadline>=today&&t.deadline<=end&&t.status!=='Completed');const events=d.events.filter(e=>e.startDate>=today&&e.startDate<=end);const work=deadlines.reduce((a,t)=>a+Math.max(0,Number(t.estimatedDuration||0)-Number(t.actualMinutes||0)),0);const risk=deadlines.some(t=>this.taskRisk(t).label==='Critical')?'High':deadlines.length>=4||work>720?'High':deadlines.length>=2||work>360?'Moderate':'Low';return{deadlines:deadlines.length,events:events.length,work,risk}}
 async renderToday(){const d=await this.data(),today=localDate(),am=this.areaMap(d.areas),cap=await this.capacity(today),s=await this.r.settings();const blocks=d.blocks.filter(b=>b.date===today).sort((a,b)=>a.startTime.localeCompare(b.startTime));const ev=d.events.filter(e=>e.startDate===today).sort((a,b)=>a.startTime.localeCompare(b.startTime));const check=d.checkins.filter(x=>x.date===today).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt))[0];const note=d.notes.find(n=>n.type==='daily'&&n.date===today);let schedule=[...ev.map(e=>({kind:'event',title:e.title,startTime:e.startTime,duration:Math.max(0,minutes(e.endTime)-minutes(e.startTime)),locked:true,areaId:e.lifeAreaId,id:e.id})),...blocks.map(b=>({kind:'block',...b}))].sort((a,b)=>a.startTime.localeCompare(b.startTime));return `<div class="section-head"><h2>${fmtDate(today,{weekday:'long',month:'long',day:'numeric'})}</h2><div class="actions"><button class="btn" data-action="checkin">Morning check-in</button><button class="btn" data-action="generate-day">Generate day</button><button class="btn" data-action="rescue-day">Rescue my day</button><button class="btn" data-action="minimum-day">Minimum day</button></div></div>
<div class="grid g3"><div class="card"><div class="muted">Capacity</div><div class="metric">${Math.round(cap.pct)}%</div><div class="progress"><span style="width:${clamp(cap.pct,0,100)}%"></span></div><div class="capacity-line"><span>Scheduled ${hm(cap.scheduled)}</span><span>Remaining ${hm(Math.max(0,cap.remaining))}</span></div></div><div class="card"><div class="muted">Morning state</div><div class="metric" style="font-size:20px">${check?`Energy ${check.energy}/10`:'Not checked in'}</div><div class="muted">${check?`Focus ${check.focus}/10 • ${escapeHTML(check.capacity||'Normal')} capacity`:'Use check-in to improve recommendations'}</div></div><div class="card"><div class="muted">Protected</div><div class="metric">${hm(cap.buffer)}</div><div class="muted">Buffer • Sleep ${escapeHTML(s.bedtime)}–${escapeHTML(s.wakeTime)}</div></div></div>
<div class="grid g2" style="margin-top:16px"><div class="card"><div class="section-head"><h2>Today timeline</h2><div class="actions"><button class="btn small" data-action="new-event">+ Event</button><button class="btn small" data-action="new-task">+ Task</button></div></div>${schedule.length?`<div class="list">${schedule.map(x=>`<div class="item"><div class="grow"><div class="item-title">${escapeHTML(x.startTime)}–${escapeHTML(timeFromMin((minutes(x.startTime)+Number(x.duration||0))%1440))} • ${escapeHTML(x.title)}</div><div class="meta"><span>${hm(x.duration)}</span><span>${escapeHTML(am[x.lifeAreaId]?.name||'Uncategorized')}</span><span>${x.locked?'🔒 Locked':'🔓 Flexible'}</span></div></div>${x.kind==='block'?`<button class="btn small" data-action="start-focus" data-id="${x.taskId||''}">Start</button><button class="btn small" data-action="delete-block" data-id="${x.id}">Remove</button>`:''}</div>`).join('')}</div>`:'<div class="empty">No blocks yet. Generate your day or add an event.</div>'}</div><div class="card"><div class="section-head"><h2>Unscheduled / available</h2></div>${this.taskList(d.tasks.filter(t=>!['Completed','Cancelled','Someday','Waiting'].includes(t.status)&&!d.blocks.some(b=>b.taskId===t.id)&&(!t.preferredDate||t.preferredDate===today)).sort((a,b)=>this.taskScore(b,{energy:Number(check?.energy||7)})-this.taskScore(a,{energy:Number(check?.energy||7)})).slice(0,8),am,{compact:true})}</div></div>
<div class="card" style="margin-top:16px"><div class="section-head"><h2>Daily note</h2><div class="actions"><button class="btn small" data-action="save-note">Save note</button><button class="btn small" data-action="review-day">End-of-day review</button></div></div><textarea id="dailyNote" style="width:100%;min-height:100px;border:1px solid var(--line);border-radius:12px;padding:10px;background:var(--panel2);color:var(--text)" placeholder="Optional notes for today…">${escapeHTML(note?.text||'')}</textarea></div>`}
 async renderCalendar(){const d=await this.data();const am=this.areaMap(d.areas);const label=this.calendarDate.toLocaleDateString(undefined,{month:'long',year:'numeric'});return `<div class="section-head"><h2>${label}</h2><div class="actions"><button class="btn small" data-action="calendar-prev">←</button><button class="btn small" data-action="calendar-today">Today</button><button class="btn small" data-action="calendar-next">→</button><button class="btn small ${this.calendarMode==='month'?'primary':''}" data-action="calendar-mode" data-mode="month">Month</button><button class="btn small ${this.calendarMode==='week'?'primary':''}" data-action="calendar-mode" data-mode="week">Week</button><button class="btn small ${this.calendarMode==='day'?'primary':''}" data-action="calendar-mode" data-mode="day">Day</button><button class="btn small ${this.calendarMode==='agenda'?'primary':''}" data-action="calendar-mode" data-mode="agenda">Agenda</button><button class="btn small" data-action="new-event">+ Event</button></div></div><div class="card">${this.calendarMode==='month'?this.monthCalendar(d,am):this.calendarMode==='week'?this.weekCalendar(d,am):this.calendarMode==='day'?this.dayCalendar(d,am):this.agendaCalendar(d,am)}</div><div class="card" style="margin-top:16px"><h3>Unscheduled tasks — drag onto a day</h3>${this.taskList(d.tasks.filter(t=>!['Completed','Cancelled','Someday','Waiting'].includes(t.status)&&!d.blocks.some(b=>b.taskId===t.id)),am,{draggable:true,compact:true})}</div>`}
 monthCalendar(d,am){const y=this.calendarDate.getFullYear(),m=this.calendarDate.getMonth();const first=new Date(y,m,1),offset=(first.getDay()+6)%7,days=new Date(y,m+1,0).getDate();let cells='';for(let i=0;i<offset;i++)cells+='<div class="cal-cell muted"></div>';for(let day=1;day<=days;day++){const date=localDate(new Date(y,m,day));const ev=d.events.filter(e=>e.startDate===date);const blocks=d.blocks.filter(b=>b.date===date);const cap=this.roughDayLoad(ev,blocks);cells+=`<div class="cal-cell ${date===localDate()?'today':''}" data-drop-date="${date}"><div class="split"><span class="day-num">${day}</span><span class="pill">${cap}%</span></div>${ev.slice(0,3).map(e=>`<div class="cal-entry" title="${escapeHTML(e.title)}">${escapeHTML(e.startTime)} ${escapeHTML(e.title)}</div>`).join('')}${blocks.slice(0,3).map(b=>`<div class="cal-entry" title="${escapeHTML(b.title)}">${escapeHTML(b.startTime)} ${escapeHTML(b.title)}</div>`).join('')}${ev.length+blocks.length>6?`<div class="muted" style="font-size:11px">+${ev.length+blocks.length-6} more</div>`:''}</div>`}return `<div class="calendar-scroll"><div class="calendar-grid">${['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(x=>`<div class="cal-head">${x}</div>`).join('')}${cells}</div></div>`}
 roughDayLoad(ev,blocks){const total=ev.reduce((a,e)=>a+Math.max(0,minutes(e.endTime)-minutes(e.startTime))+Number(e.travelBefore||0)+Number(e.travelAfter||0)+Number(e.preparationTime||0),0)+blocks.reduce((a,b)=>a+Number(b.duration||0),0);return Math.round(total/(16*60)*100)}
 weekCalendar(d,am){const mon=startOfWeek(this.calendarDate);const dates=Array.from({length:7},(_,i)=>localDate(new Date(mon.getFullYear(),mon.getMonth(),mon.getDate()+i)));let html=`<div class="week-grid"><div class="week-head"></div>${dates.map(x=>`<div class="week-head"><b>${fmtDate(x,{weekday:'short'})}</b><br><span class="muted">${fmtDate(x,{month:'short',day:'numeric'})}</span></div>`).join('')}`;for(let h=7;h<=22;h++){html+=`<div class="week-time">${String(h).padStart(2,'0')}:00</div>`;for(const date of dates){const ev=d.events.filter(e=>e.startDate===date&&Number(e.startTime.slice(0,2))===h);const bl=d.blocks.filter(b=>b.date===date&&Number(b.startTime.slice(0,2))===h);html+=`<div class="week-cell" data-drop-date="${date}">${[...ev,...bl].map(x=>`<div class="cal-entry">${escapeHTML(x.startTime)} ${escapeHTML(x.title)}</div>`).join('')}</div>`}}return html+'</div>'}
 dayCalendar(d,am){const date=localDate(this.calendarDate);const all=[...d.events.filter(e=>e.startDate===date).map(e=>({title:e.title,start:e.startTime,end:e.endTime,locked:true,id:e.id,kind:'event'})),...d.blocks.filter(b=>b.date===date).map(b=>({title:b.title,start:b.startTime,end:timeFromMin(minutes(b.startTime)+Number(b.duration||0)),locked:b.locked,id:b.id,kind:'block'}))].sort((a,b)=>a.start.localeCompare(b.start));return `<h3>${fmtDate(date,{weekday:'long',month:'long',day:'numeric'})}</h3>${all.length?`<div class="list">${all.map(x=>`<div class="item"><div class="grow"><div class="item-title">${escapeHTML(x.start)}–${escapeHTML(x.end)} • ${escapeHTML(x.title)}</div><div class="meta"><span>${x.locked?'🔒 Locked':'🔓 Flexible'}</span></div></div>${x.kind==='event'?`<button class="btn small" data-action="edit-event" data-id="${x.id}">Edit</button>`:`<button class="btn small" data-action="delete-block" data-id="${x.id}">Remove</button>`}</div>`).join('')}</div>`:'<div class="empty">No schedule for this day.</div>'}`}
 agendaCalendar(d,am){const today=localDate(),end=addDays(today,30);const entries=[...d.events.filter(e=>e.startDate>=today&&e.startDate<=end).map(e=>({date:e.startDate,time:e.startTime,title:e.title,type:'Event'})),...d.tasks.filter(t=>t.deadline&&t.deadline>=today&&t.deadline<=end&&t.status!=='Completed').map(t=>({date:t.deadline,time:'23:59',title:t.title,type:'Deadline'}))].sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time));return entries.length?`<div class="list">${entries.map(x=>`<div class="item"><div class="grow"><div class="item-title">${escapeHTML(x.title)}</div><div class="meta"><span>${x.type}</span><span>${fmtDate(x.date)}</span><span>${x.time}</span></div></div></div>`).join('')}</div>`:'<div class="empty">Nothing scheduled in the next 30 days.</div>'}
 moveCalendar(n){if(this.calendarMode==='month')this.calendarDate.setMonth(this.calendarDate.getMonth()+n);else if(this.calendarMode==='week')this.calendarDate.setDate(this.calendarDate.getDate()+n*7);else this.calendarDate.setDate(this.calendarDate.getDate()+n)}
 async renderTasks(){const d=await this.data(),am=this.areaMap(d.areas);let list=d.tasks;const today=localDate();const tab=this.taskTab;if(tab==='Inbox')list=list.filter(t=>t.status==='Inbox');else if(tab==='Today')list=list.filter(t=>t.preferredDate===today||d.blocks.some(b=>b.date===today&&b.taskId===t.id));else if(tab==='Next')list=list.filter(t=>['Next','Scheduled','In Progress','Postponed'].includes(t.status));else if(tab==='Upcoming')list=list.filter(t=>t.deadline&&t.deadline>=today&&t.status!=='Completed');else if(tab==='Overdue')list=list.filter(t=>t.deadline&&t.deadline<today&&t.status!=='Completed');else if(tab==='Unscheduled')list=list.filter(t=>!d.blocks.some(b=>b.taskId===t.id)&&!['Completed','Cancelled','Someday','Waiting'].includes(t.status));else if(tab==='Waiting')list=list.filter(t=>t.status==='Waiting');else if(tab==='Someday')list=list.filter(t=>t.status==='Someday');else if(tab==='Completed')list=list.filter(t=>t.status==='Completed');list.sort((a,b)=>this.taskScore(b)-this.taskScore(a));return `<div class="section-head"><h2>Tasks</h2><div class="actions"><button class="btn primary" data-action="new-task">+ Task</button></div></div><div class="tabs">${['Inbox','Today','Next','Upcoming','Overdue','Unscheduled','Waiting','Someday','Completed'].map(x=>`<button class="${tab===x?'active':''}" data-action="task-tab" data-tab="${x}">${x}</button>`).join('')}</div>${this.taskList(list,am)}`}
 taskList(list,am,opt={}){if(!list.length)return'<div class="empty">No items here yet.</div>';return `<div class="list">${list.map(t=>{const risk=this.taskRisk(t);const area=am[t.lifeAreaId];return `<div class="item drag-task" ${opt.draggable?'draggable="true"':''} data-task="${t.id}"><div class="grow"><div class="item-title">${escapeHTML(t.title)}</div><div class="meta">${priorityPill(t.priority)}<span>${escapeHTML(area?.icon||'•')} ${escapeHTML(area?.name||'Uncategorized')}</span><span>${hm(Number(t.estimatedDuration||0))}</span>${t.deadline?`<span class="${risk.class}">Due ${fmtDate(t.deadline)} • ${risk.label}</span>`:''}<span>${escapeHTML(t.status||'Next')}</span>${t.context?`<span>${escapeHTML(t.context)}</span>`:''}${(t.blockedBy||[]).length?'<span class="status-warn">Blocked</span>':''}</div></div>${opt.compact?'':`<div style="display:flex;gap:5px;flex-wrap:wrap;justify-content:flex-end"><button class="btn small" data-action="start-focus" data-id="${t.id}">Focus</button><button class="btn small" data-action="complete-task" data-id="${t.id}">Done</button><button class="btn small" data-action="postpone-task" data-id="${t.id}">Postpone</button><button class="btn small" data-action="edit-task" data-id="${t.id}">Edit</button><button class="btn small" data-action="delete-task" data-id="${t.id}">Delete</button></div>`}</div>`}).join('')}</div>`}
 async renderProjects(){const d=await this.data(),am=this.areaMap(d.areas),gm=Object.fromEntries(d.goals.map(g=>[g.id,g]));return `<div class="section-head"><h2>Projects</h2><div class="actions"><button class="btn primary" data-action="new-project">+ Project</button></div></div>${d.projects.length?`<div class="grid g3">${d.projects.map(p=>{const tasks=d.t