name: Apply DocFlow migration bootstrap (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy bootstrap is disabled because its integrity checksum does not match. Use the modular migration commits instead."
